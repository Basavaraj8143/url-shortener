# URL Shortener Project Journal

## Day 1 - Project Initialization

### Objective
Set up the Spring Boot project and understand the generated structure.

### Steps Performed

1. Opened Spring Initializr.
2. Created a new Spring Boot project.

---

### Project Configuration

```text
Project Type : Maven
Language     : Java
Spring Boot  : Latest Stable Version
Group        : com.busss
Artifact     : url-shortener
Name         : url-shortener
Packaging    : Jar
Java Version : 21
```

### Dependencies Added

- Spring Web
- Spring Data JPA
- MySQL Driver
- Lombok
- Validation

---

### Generated Project Structure

```
url-shortener
│
├── .mvn
├── src
│   ├── main
│   │   ├── java
│   │   │   └── com.busss.url_shortener
│   │   │       └── UrlShortenerApplication.java
│   │   │
│   │   └── resources
│   │
│   └── test
│       └── java
│           └── com.busss.url_shortener
│               └── UrlShortenerApplicationTests.java
│
├── pom.xml
├── mvnw
├── mvnw.cmd
├── .gitignore
└── HELP.md
```

---

### What Each Generated File Does

#### `UrlShortenerApplication.java`
Main entry point of the Spring Boot application.

```java
@SpringBootApplication
public class UrlShortenerApplication {

    public static void main(String[] args) {
        SpringApplication.run(
            UrlShortenerApplication.class,
            args
        );
    }
}
```

Responsibilities:
- Starts Spring Boot
- Creates Spring Container
- Starts Embedded Tomcat Server
- Scans Components and Beans

---

#### `UrlShortenerApplicationTests.java`
Default testing class generated by Spring Boot.

Purpose:
- Used for unit and integration testing.
- Verifies application context loads correctly.

> Currently not modified.

---

#### `pom.xml`
Maven build configuration file.

Purpose:
- Manages dependencies
- Defines project metadata
- Controls build process

Current dependencies:
- Spring Web
- Spring Data JPA
- MySQL Driver
- Lombok
- Validation

---

#### `resources/`
Stores:
- `application.properties`
- Static files
- Configuration files

Will later contain:

```properties
spring.datasource.url=...
spring.datasource.username=...
spring.datasource.password=...
```

---

#### `.mvn` + `mvnw` + `mvnw.cmd`
Maven Wrapper files.

Purpose:
- Allows project build without installing Maven globally.
- Ensures same Maven version across systems.

---

### Concepts Learned

- What Spring Initializr is
- Maven project structure
- Spring Boot entry point
- Embedded Tomcat server
- Dependency management using `pom.xml`
- Standard enterprise package structure

---

### Status

✅ Spring Boot project initialized successfully

### Next Step

Create package structure:

```
controller/
service/
repository/
entity/
dto/
exception/
config/
```